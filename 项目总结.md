# 番茄钟 v2 项目总结

## 概览
Windows 绿色便携版番茄钟，Electron 28 + 纯 vanilla JS（无框架），打包为单个 .exe。

## 架构（三进程模型）

| 层 | 文件 | 行数 | 职责 |
|---|---|---|---|
| 主进程 | `main.js` | 144 | 窗口管理、托盘、IPC、JSON 读写 |
| 预加载 | `preload.js` | 16 | contextBridge 暴露 `window.api`（3 个命名空间） |
| 渲染进程 | `renderer/` 共 8 文件 | ~950 | 纯前端，ES Module 架构 |

## 渲染进程模块依赖

```
app.js（入口）
  ├── taskStore.js  — 数据层（CRUD + v1→v2 迁移 + 持久化）
  ├── timer.js      — 计时器状态机（work→short-break→long-break 循环）
  ├── sound.js      — Web Audio API 合成提示音
  ├── dialog.js     — 任务编辑弹窗（Promise-based）
  └── ui.js         — DOM 渲染、事件处理、关闭弹窗
```

## v2 新增功能（3 个阶段全部完成）

| 阶段 | 内容 | Commit | 审查 |
|---|---|---|---|
| 1 | 窗口可调整大小 + 尺寸持久化 + close IPC | `1cb0714` | 0 问题 |
| 2 | 模块拆分 + 任务列表 + 自定义时间 + 数据迁移 | `2d88911` | 3 中等 / 2 轻微 |
| 3 | 关闭确认弹窗 + UI 打磨 | `ad545e0` | 0 问题 |

## 数据模型（v2）

```json
{
  "tasks": [{ "id", "name", "workMinutes", "shortBreakMinutes", "longBreakMinutes", "completedPomodoros", "createdAt" }],
  "history": [{ "taskId", "taskName", "date", "completedAt" }],
  "activeTaskId": null,
  "windowBounds": { "width": 450, "height": 650 }
}
```

启动时自动检测 v1 格式并迁移，迁移前自动备份 `.bak`。

## IPC 通道（6 个）

| 通道 | 类型 | 用途 |
|---|---|---|
| `data:load` | invoke (双向) | 读取 JSON 数据 |
| `data:save` | invoke (双向) | 保存 JSON 数据 |
| `data:backup` | invoke (双向) | 备份数据文件 |
| `notification:send` | invoke (双向) | 系统通知 |
| `window:minimize` | send (单向) | 最小化窗口 |
| `window:close` | send (单向) | 真正关闭（非隐藏到托盘） |

## 关键设计决策

- **3 阶段而非 4 阶段** — 芒格审查后合并，避免无测试的纯重构
- **数据迁移在阶段2** — 不是最后阶段，迁移前自动备份
- **Git 直接在 main 上 commit** — 一人项目不需要分支策略
- **ES Module 而非 IIFE** — index.html 用 type="module"
- **关闭弹窗不复用 dialog.js** — dialog.js 是任务编辑弹窗，关闭弹窗在 ui.js 处理
- **今日番茄数从 history 统计** — 不单独存 todayCount

## 技术债（Phase 2 审查发现，未修复）

1. `timer.js:130` — `incrementCompletedPomodoros()` 是死代码（`advanceAfterComplete()` 已内部处理）
2. `main.js:22` — `loadData()` 默认值仍是 v1 格式，新装会触发不必要迁移
3. `taskStore.js:52-53,58-59` — `catch {}` 静默吞错误，数据持久化失败不可见
4. `ui.js:132-137` — 删除任务后计时器未重置到默认阶段/时长
5. `app.js:46-48` — `onPhaseChange` 调用 `updateTimerDisplay(0, totalSeconds)` 导致短暂闪 `00:00`

## 未执行

- `npm start` 尚未实际运行测试
- `npm run build` 尚未打包 exe
- `project-log.md` 有未提交的修改

## 其他信息

- **启动方式**：`npm start`（通过 start.js 清除 ELECTRON_RUN_AS_NODE）
- **打包命令**：`npm run build` → `electron-builder --win portable` → `dist/PomodoroTimer.exe`
- **Node/Electron**：Electron 28.3.3
- **数据文件**：`pomodoro-data.json`（开发时存项目目录，打包后存 exe 同目录）
- **当前数据**：包含一个"默认任务"，0 完成番茄，无历史记录

---

## 结论

仅做项目总结，不做代码改动。
